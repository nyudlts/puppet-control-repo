# managed by puppet
---
:backends:
- yaml

:logger: console

:hierarchy:
  - "%{environment}/%{calling_class}"
  - "%{environment}"
  - accounts
  - common

:yaml:
  :datadir: "/etc/puppetlabs/code/environments/%{::environment}/hieradata"

